# پیاده سازی تست آنلاین کلیفتون

## ساختار داده (Data Structure)

در این پروژه، داده‌های تست کلیفتون به دو دسته‌ی اصلی تقسیم می‌شوند:

1.  **داده‌های استاتیک (Static Data)**
    
2.  **داده‌های پویا و نتایج کاربران (User Results Data)**
    

در ادامه، ساختار، منبع و نحوه‌ی مدیریت هرکدام توضیح داده می‌شود.

----------

## نسخه اولیه داده‌ها (Raw JSON Resources)

تمام داده‌های مورد نیاز برای پر کردن جداول استاتیک دیتابیس در **سه فایل JSON** در مسیر زیر نگهداری می‌شوند:

`api/resources/` 

### فایل‌ها و توضیحات
| فایل | توضیح |
|--|--|
| `domains.json` | شامل رکوردهای مربوط به **دامین‌ها (Domains)** است. هر رکورد شامل کلید یکتا (`domainKey`)، نام، آیکون، توضیحات سه سطحی (`low`, `average`, `high`)، حداکثر امتیاز (`maxDegree`) و رنگ‌های مرتبط با UI است. این داده‌ها در جدول `clifton_domains` ذخیره می‌شوند. |
| `themes.json` | شامل اطلاعات کامل **تم‌های استعدادی (Themes)** است. هر رکورد شامل کلید یکتا (`themeKey`)، دامین مرتبط، نام، آیکون، جمله شاخص تم (`phrase`) و توضیحات متنی و ساختاری دیگر می‌باشد. بخش‌های مختلفی از داده‌ها مانند `positiveIdentifiers`, `negativeIdentifiers`, `howThemeHelpsYou`, `behavioralProfile`, `workplace`, `improvementMethods`, `books`, `onWeaknessObstacles`, `onWeaknessOpportunities` به صورت **JSON** در جدول `clifton_themes` ذخیره می‌شوند. |
| `questions.json` | شامل داده‌های **سوالات تست** است. هر سوال شامل **دو عبارت (Phrase A و Phrase B)** و تم‌های مرتبط با هر عبارت می‌باشد. این داده‌ها در جدول `clifton_questions` ذخیره می‌شوند. |




> این سه فایل JSON به عنوان **source of truth** برای داده‌های استاتیک عمل می‌کنند و از آن‌ها برای پر کردن جدول‌های دیتابیس استفاده می‌شود.

### نکات مهم

-   ساختار هر فایل با توجه به partialهای مربوطه در اسکریپت `manage_tables.php` پردازش می‌شود.
    
-   هر تغییر در این فایل‌ها باید با دقت انجام شود، زیرا مستقیماً در دیتابیس اعمال خواهد شد.
    
-   پس از به‌روزرسانی فایل‌ها، برای اعمال تغییرات باید گزینه `--add-data` را هنگام اجرای اسکریپت استفاده کنید.



# مدیریت جداول و وارد کردن داده‌ها

در این پروژه، تمام عملیات مربوط به ایجاد جداول و وارد کردن داده‌های اولیه به دیتابیس با یک اسکریپت مرکزی انجام می‌شود:

`api/scripts/manage_tables.php` 


----------

## جدول‌ها و فایل‌های منبع

### جداول دیتابیس

-   **clifton_domains** : شامل اطلاعات دامین‌های تست (Domains)
    
-   **clifton_themes** : شامل اطلاعات تم‌های استعدادی (Themes)
    
-   **clifton_questions** : شامل داده‌های سوالات تست
    

> جدول `clifton_results` برای ذخیره نتایج کاربران جداگانه و پویا است و توسط این اسکریپت مدیریت نمی‌شود.

### فایل‌های JSON منابع

مسیر فایل‌های JSON برای هر جدول به صورت hardcode در اسکریپت تعریف شده است:

جدول

فایل JSON

clifton_domains

`api/resources/domains.json`

clifton_themes

`api/resources/themes.json`

clifton_questions

`api/resources/questions.json`

> این فایل‌ها حاوی داده‌های اولیه هستند و توسط partialهای مربوطه به جدول‌ها اضافه می‌شوند.

----------

## Partialها (Insert Handlers)

برای هر جدول یک partial وجود دارد که عملیات **خواندن JSON و وارد کردن داده‌ها** را انجام می‌دهد:

-   `partials/insert_domains.php`  
    پردازش داده‌ها و وارد کردن اطلاعات دامین‌ها به جدول `clifton_domains`.
    
-   `partials/insert_themes.php`  
    پردازش داده‌ها و وارد کردن اطلاعات تم‌ها به جدول `clifton_themes`.
    
    -   فیلدهای JSON شامل `positiveIdentifiers`, `negativeIdentifiers`, `howThemeHelpsYou`, `behavioralProfile`, `workplace`, `improvementMethods`, `books`, `onWeaknessObstacles`, `onWeaknessOpportunities`.
        
-   `partials/insert_questions.php`  
    پردازش داده‌ها و وارد کردن سوالات به جدول `clifton_questions`.
    
    -   توجه: هر سوال شامل Phrase A و Phrase B است و هر Phrase می‌تواند به چند تم مرتبط باشد.
        

----------

## نحوه اجرای اسکریپت

اسکریپت باید از طریق **CLI / Shell سرور** اجرا شود (چه روی لوکال و چه سرور آنلاین).

### فرمت اجرای کلی

`php manage_tables.php --db-pass=<DB_PASSWORD> [--create-table] [--add-data] [--themes|--domains|--questions]` 

### مثال‌ها

-   ایجاد جدول تم‌ها:
    

`php manage_tables.php --db-pass=secret --create-table --themes` 

-   اضافه کردن داده‌ها به جدول دامین‌ها:
    

`php manage_tables.php --db-pass=secret --add-data --domains` 

-   ایجاد همه جداول و وارد کردن داده‌های همه جدول‌ها:
    

`php manage_tables.php --db-pass=secret --create-table --add-data` 

----------

## گزینه‌ها (Options)


| Option | توضیح |
|--|--|
| `--db-pass=<password>` | **الزامی**؛ پسورد اتصال به دیتابیس |
| `--create-table` | ایجاد جدول(ها) مشخص یا همه جدول‌ها |
| `--add-data` | وارد کردن داده‌ها از فایل JSON مربوطه |
| `--themes` | انتخاب جدول تم‌ها برای اجرا |
| `--domains` | انتخاب جدول دامین‌ها برای اجرا |
| `--questions` | انتخاب جدول سوالات برای اجرا |


انتخاب جدول سوالات برای اجرا

> اگر هیچ جدول خاصی مشخص نشود، اسکریپت تمام جدول‌ها را پردازش می‌کند.  
> فقط یک جدول می‌تواند در یک اجرای اسکریپت پردازش شود؛ اگر چند جدول همزمان انتخاب شود، اسکریپت خطا می‌دهد.

----------

## نکات مهم

-   مسیر فایل‌های JSON و اطلاعات اتصال به دیتابیس (`DB_USER`, `DB_HOST`, `DB_NAME`) به صورت **hardcode** در اسکریپت تعریف شده‌اند. تنها پسورد باید هنگام اجرا وارد شود.
    
-   قبل از اجرای اسکریپت روی سرور جدید، مسیر فایل‌ها و اطلاعات دیتابیس را بررسی و در صورت نیاز اصلاح کنید.
    
-   partialها به‌صورت امن عملیات `INSERT ... ON DUPLICATE KEY UPDATE` را انجام می‌دهند تا داده‌های موجود بروزرسانی شوند و از ایجاد رکوردهای تکراری جلوگیری شود.
    
-   تمام داده‌های ساختاریافته (مانند فیلدهای JSON در تم‌ها و سوالات) قبل از وارد کردن به دیتابیس encode می‌شوند.

----------

## ساختار جداول دیتابیس

### جدول `clifton_domains`

این جدول اطلاعات مربوط به دامین‌های اصلی (Domains) مدل CliftonStrengths را نگهداری می‌کند.

**کاربرد:**  
نمایش دسته‌بندی‌های کلان تم‌ها، توضیحات سطح تسلط و تنظیمات ظاهری مرتبط با هر دامین.

**فیلدهای کلیدی:**

-   `domainKey` : کلید یکتای دامین
    
-   `name` : نام دامین
    
-   `descriptionLow / Average / High` : توضیحات سطح تسلط
    
-   `maxDegree` : حداکثر امتیاز ممکن برای دامین
    
-   `colorPrimary`, `colorBg`, `colorSep` : تنظیمات رنگی UI
    

----------

### جدول `clifton_themes`

این جدول شامل اطلاعات کامل هر تم استعدادی (Theme) می‌باشد.

**کاربرد:**  
تحلیل شخصیت، نمایش توضیحات، پیشنهادها، کتاب‌ها و پروفایل رفتاری هر تم.

**فیلدهای کلیدی:**

-   `themeKey` : کلید یکتای تم
    
-   `domain` : دامین مرتبط با تم
    
-   `phrase` : جمله‌ی شاخص تم
    
-   `onDominanceDescription` : توضیح زمانی که تم غالب است
    
-   `positiveIdentifiers / negativeIdentifiers` : نشانه‌های مثبت و منفی
    
-   `behavioralProfile`, `workplace`, `improvementMethods` : اطلاعات تحلیلی
    
-   `books` : منابع مطالعاتی پیشنهادی
    
-   `maxDegree` : حداکثر امتیاز تم
    

بخش قابل‌توجهی از داده‌ها به‌صورت `JSON` ذخیره می‌شوند تا انعطاف‌پذیری بالاتری در ساختار محتوا وجود داشته باشد.

----------

### جدول `clifton_questions`

این جدول سوالات تست را ذخیره می‌کند.

**ساختار هر سوال:**

-   دو عبارت (Phrase A و Phrase B)
    
-   هر عبارت می‌تواند به چند تم مرتبط باشد
    

**فیلدهای کلیدی:**

-   `phrase_a_text`
    
-   `phrase_a_themes` (JSON)
    
-   `phrase_b_text`
    
-   `phrase_b_themes` (JSON)
    

این ساختار امکان امتیازدهی چندتمی به هر انتخاب را فراهم می‌کند.

----------

## جدول نتایج کاربران (`clifton_results`)

این جدول برای ذخیره اطلاعات کاربران و نتایج نهایی تست استفاده می‌شود و جزو داده‌های **پویا** محسوب می‌شود.

**کاربرد:**

-   ذخیره امتیازات نهایی هر تم
    
-   نگهداری اطلاعات کاربر
    
-   پشتیبانی از انواع مختلف تست (رایگان، پولی و …)
    

**فیلدهای کلیدی:**

-   `user_id` : شناسه کاربر (در صورت وجود)
    
-   `mobile_number` : شماره موبایل کاربر
    
-   `degrees` : امتیازات نهایی تم‌ها (JSON یا متن سریالایز شده)
    
-   `user_info` : اطلاعات تکمیلی کاربر
    
-   `test_option` : نوع تست (مثلاً `free`)
    
-   ایندکس‌ها برای بهبود performance جستجو بر اساس موبایل و کاربر

---
## نحوه لود و مدیریت سوالات در صفحه `index.php`

صفحه `index.php` به‌عنوان نقطه شروع تست کلیفتون، وظیفه دریافت و نمایش سوالات را به‌صورت پویا از سمت سرور بر عهده دارد. فرآیند لود سوالات به شکل زیر انجام می‌شود:

---

### ۱. شروع فرآیند لود سوالات

* هم‌زمان با لود شدن صفحه `index.php`، تابعی با نام:

```
loadQuestions()
```

* این تابع در فایل زیر تعریف شده است:

```
assets/js/main.js
```

* اجرای این تابع نقطه شروع فرآیند دریافت سوالات از API محسوب می‌شود

---

### ۲. ارسال درخواست به API

* تابع `loadQuestions` یک **AJAX Request** به API پروژه ارسال می‌کند
* هدف این درخواست:

  * دریافت لیست کامل سوالات تست
  * دریافت سوالات با فرمت استاندارد قابل استفاده در فرانت‌اند

---

### ۳. پردازش سوالات در سمت سرور

* در سمت سرور، تابعی با نام:

```
clifton_questions
```

مسئول پردازش این درخواست است

* وظایف این تابع عبارت‌اند از:

  1. **دریافت سوالات از دیتابیس** از جدول `clifton_questions`
  2. **فرمت‌بندی داده‌ها** مطابق با ساختار مورد انتظار فرانت‌اند

     * شامل متن عبارات
     * تم‌های مرتبط با هر عبارت و وزن‌های آن‌ها
  3. **ارسال داده‌های آماده‌شده به سمت فرانت‌اند** به صورت JSON

---

### ۴. دریافت و ذخیره سوالات در فرانت‌اند

* پس از دریافت پاسخ از API:

  * داده‌ها در فرانت‌اند **decode (parse)** می‌شوند
  * لیست سوالات در یک متغیر لوکال با نام:

```js
questions
```

ذخیره می‌شود

* این متغیر در طول اجرای برنامه مورد استفاده قرار می‌گیرد، از جمله برای:

  * نمایش سوالات
  * مدیریت پاسخ‌های کاربر
  * محاسبه امتیازها در پایان تست

---

### نکات مهم

* سوالات تنها یک‌بار در ابتدای لود صفحه دریافت می‌شوند
* استفاده از متغیر لوکال `questions` باعث:

  * کاهش درخواست‌های غیرضروری به سرور
  * بهبود عملکرد و سرعت تست
* هرگونه تغییر در سوالات دیتابیس، بدون نیاز به تغییر در فرانت‌اند، در لود بعدی صفحه اعمال خواهد شد

---



## نحوه محاسبه پاسخ‌ها و نتایج تست

### ساختار سوالات

-   هر **سوال** در تست شامل **دو عبارت (Phrase A و Phrase B)** است.
    
-   کاربر باید انتخاب کند که **کدام عبارت و با چه درجه‌ای او را توصیف می‌کند**.
    
-   هر عبارت دارای **درجه یا وزن انتخابی توسط کاربر** است که نقش **ضریب امتیاز تم‌ها** را دارد.
    

### ارتباط عبارات با تم‌ها

-   در دیتابیس، هر عبارت شامل یک **لیست key-value** است که مشخص می‌کند:
    
    -   این عبارت به کدام **تم‌ها** مرتبط است
        
    -   هر تم چه **وزنی (weight)** دارد
        
-   ساختار مثال در جدول `clifton_questions`:
    

``` json
{  
  "phrase_a_text":  "من ترجیح می‌دهم کارها را برنامه‌ریزی شده انجام دهم",  
  "phrase_a_themes":  {  "Discipline":  2,  "Consistency":  1  },  
  "phrase_b_text":  "من انعطاف‌پذیری بیشتری در کارها دارم",  
  "phrase_b_themes":  {  "Adaptability":  2  }  
  }
```

> در این مثال، اگر کاربر **Phrase A را با درجه 2 انتخاب کند**، امتیاز نهایی تم‌ها به صورت زیر محاسبه می‌شود:
> 
> -   `Discipline`: 2 (وزن تم) × 2 (درجه انتخاب کاربر) = 4
>     
> -   `Consistency`: 1 × 2 = 2
>     

### فرآیند محاسبه امتیازات

1.  برای هر سوال، **انتخاب کاربر** بررسی می‌شود.
    
2.  **درجه انتخاب کاربر** برای عبارت استخراج می‌شود.
    
3.  برای هر تم مرتبط با عبارت، امتیاز تم **ضرب در وزن تم در ساختار JSON** محاسبه شده و به مجموع امتیازات تم اضافه می‌شود:
    

امتیاز تم نهایی=وزن تم در JSON×درجه انتخاب کاربر\text{امتیاز تم نهایی} = \text{وزن تم در JSON} \times \text{درجه انتخاب کاربر}امتیاز تم نهایی=وزن تم در JSON×درجه انتخاب کاربر

4.  این فرآیند برای تمام سوالات تکرار می‌شود تا **مجموع امتیازات نهایی هر تم** محاسبه گردد.
    

### تعیین تم‌های برتر

-   پس از جمع‌آوری امتیازات از تمام سوالات، **تم‌ها با بیشترین امتیاز** به عنوان **تم‌های برتر کاربر** معرفی می‌شوند.
    
-   این تم‌ها در نتیجه نهایی تست نمایش داده می‌شوند و می‌توانند برای تحلیل شخصیت، پیشنهاد مسیر شغلی یا توصیه‌های توسعه فردی مورد استفاده قرار گیرند.

---

## ساختار فرانت‌اند و رندر داده‌ها در صفحه نتایج تست کلیفتون

صفحه نتایج تست کلیفتون به‌عنوان مهم‌ترین خروجی سیستم، وظیفه نمایش تحلیل نهایی کاربر را بر عهده دارد. این صفحه بر اساس داده‌های پردازش‌شده تست و محتوای استاتیک موجود در دیتابیس طراحی شده و شامل **۱۰ تب اصلی** است که هرکدام بخشی از تحلیل شخصیت کاربر را نمایش می‌دهند.

----------

## تب ۱: خلاصه نتایج

این تب، نمای کلی و سریع از نتیجه تست را در اختیار کاربر قرار می‌دهد و شامل اطلاعات زیر است:

-   **غالب‌ترین استعداد (Top Theme)**
    
-   **ضعیف‌ترین استعداد (Lowest Theme)**
    
-   **غالب‌ترین دامنه استعدادی (Top Domain)**
    

هدف این بخش، ارائه یک دید سریع و قابل فهم از وضعیت کلی شخصیت کاربر پیش از ورود به جزئیات است.

----------

## تب ۲: استعدادهای غالب

این تب شامل یک لیست کامل از **تمام ۳۴ تم استعدادی** است که:

-   به‌صورت **مرتب‌شده بر اساس میزان غلبه (امتیاز نهایی)**
    
-   به شکل **نزولی (از بیشترین به کمترین)** نمایش داده می‌شوند.
    

### ساختار نمایشی این بخش

-   شامل **۳۴ آیکون‌باکس** است
    
-   برای **۱۰ تم با بالاترین امتیاز**، اطلاعات زیر نمایش داده می‌شود:
    
    -   نام تم استعدادی
        
    -   آیکون اختصاصی تم
        
    -   عبارت شاخص (Phrase) تم
        
-   طراحی هر آیکون‌باکس با **رنگ دامنه (Domain) مرتبط با آن تم** انجام شده است
    

این بخش به کاربر کمک می‌کند جایگاه نسبی تمام استعدادهای خود را به‌صورت بصری و مقایسه‌ای درک کند.

----------

## تب ۳: زمینه‌های استعدادی شما (Domains Overview)

این تب به بررسی عملکرد کاربر در **۴ دامنه اصلی استعدادی** می‌پردازد.

### ساختار کلی

-   شامل **۴ سکشن اصلی** (هر سکشن مربوط به یک دامنه استعدادی)
    
-   **ترتیب سکشن‌ها** بر اساس **امتیاز نهایی هر دامنه** و به صورت **صعودی** است
    

### ساختار هر سکشن دامنه

هر سکشن شامل **دو ستون اصلی** است:

#### ستون اول: معرفی دامنه

شامل اطلاعات زیر است:

-   آیکون اختصاصی دامنه
    
-   نام دامنه
    
-   **درصد غلبه دامنه** که با رنگ اختصاصی همان دامنه طراحی شده است
    

درصد غلبه دامنه به سه سطح تقسیم می‌شود:

-   غلبه کم: `0% – 30%`
    
-   غلبه متوسط: `30% – 60%`
    
-   غلبه بالا: `60% – 100%`
    

> متن توضیح و معرفی دامنه **بسته به سطح غلبه کاربر** متفاوت است.  
> این توضیحات در ساختار داده دامنه در دیتابیس (فیلدهای `descriptionLow`، `descriptionAverage` و `descriptionHigh`) مشخص شده‌اند.

#### ستون دوم: تحلیل تم‌های دامنه

-   نمایش **درصد غلبه تمام تم‌های مرتبط با دامنه مورد نظر**
    
-   داده‌ها به صورت **چارت و نمودار** رندر می‌شوند تا مقایسه بصری بین تم‌ها امکان‌پذیر باشد
    

----------

## تب‌های ۴ تا ۸: بررسی استعداد اول تا پنجم

این پنج تب به بررسی عمیق **۵ تم استعدادی برتر کاربر** می‌پردازند.  
تمام این تب‌ها از نظر ساختار و طراحی **کاملاً مشابه** هستند و فقط داده‌های آن‌ها تغییر می‌کند.

----------

### ساختار کلی هر تب بررسی استعداد

#### 1. معرفی اولیه تم

-   نمایش **تصویر شاخص تم** (vector-style illustration)
    
-   نمایش یک متن معرفی عمومی درباره تم استعدادی مورد نظر
    

----------

#### 2. سطح و درصد غلبه تم

-   نمایش درصد و سطح غلبه تم به صورت **چارت یا نمودار**
    
-   نمایش یک متن توضیحی که تکمیل‌کننده معرفی تم است و توضیح می‌دهد:
    
    -   اگر این تم در کاربر غالب باشد، چه ویژگی‌های شخصیتی محتمل است
        

در بالای این بخش، **۳ کارت اطلاعاتی** نمایش داده می‌شود که شامل:

-   نام انگلیسی تم
    
-   دامنه (Domain) تم
    
-   رتبه تم در بین ۳۴ استعداد کاربر
    

----------

#### 3. نقاط قوت و نقاط ضعف تم

-   نمایش مجموعه‌ای از کلیدواژه‌ها (Keywords)
    
-   شامل:
    
    -   نقاط قوت (Positive Identifiers)
        
    -   نقاط ضعف (Negative Identifiers)
        

> محتوای این بخش از دیتابیس و از کلیدهای  
> `positiveIdentifiers` و `negativeIdentifiers` خوانده می‌شود.

----------

#### 4. چگونه استعداد شما می‌تواند به شما کمک کند

-   توضیحاتی کاربردی درباره تأثیر مثبت این استعداد در زندگی فردی و حرفه‌ای
    
-   محتوای این بخش از دیتابیس و کلید `howThemeHelpsYou` دریافت می‌شود
    

----------

#### 5. پروفایل رفتاری استعداد شما

این بخش به بررسی رفتار احتمالی کاربر در سناریوهای مختلف می‌پردازد و شامل **۴ کارت** است:

-   واکنش تحت فشار (`pressure`)
    
-   انگیزه‌ها (`motivation`)
    
-   شیوه تصمیم‌گیری (`decision_making`)
    
-   سبک یادگیری (`learning`)
    

> این داده‌ها در ساختار تم استعدادی تحت کلید `behavioralProfile` ذخیره شده‌اند.

----------

#### 6. عملکرد تم در محیط کاری و تیمی

-   بررسی نقاط قوت و ضعف احتمالی این تم در محیط کار
    
-   شامل:
    
    -   نقاط قوت (`strengths`)
        
    -   نقاط ضعف (`weaknesses`)
        

> این داده‌ها از کلید `workplace` در دیتابیس استخراج می‌شوند.

----------

#### 7. اقدامات کلیدی برای توسعه استعداد

-   ارائه راهکارهایی عملی برای تبدیل استعداد به توانمندی
    
-   محتوای این بخش از دیتابیس و کلید `improvementMethods` دریافت می‌شود
    

⚠️ **نکته مهم:**  
محتوای این بخش به صورت **HTML** ذخیره شده است (نه متن خام) و در فرانت‌اند به صورت امن رندر میشود.

----------

#### 8. معرفی کتاب‌های پیشنهادی

این بخش شامل لیستی از کتاب‌های پیشنهادی مرتبط با تم استعدادی است.

ساختار نمایش هر کتاب:

-   ستون اول: تصویر کتاب
    
-   ستون دوم:
    
    -   نام کتاب
        
    -   نام نویسنده
        
    -   توضیح اینکه این کتاب چگونه می‌تواند برای کاربر مفید باشد
        

> محتوای این بخش از کلید `books` در دیتابیس خوانده می‌شود.  
> هر رکورد کتاب شامل فیلدهای `bookTitle`، `bookAuthor`، `description` و `picture` است.

## تب ۹: بررسی استعدادهای فرعی (Secondary Themes Overview)

این تب به تحلیل و معرفی **۵ تم استعدادی رتبه ۶ تا ۱۰ کاربر** می‌پردازد و شامل دو بخش اصلی است: **پیش‌نمایش تم‌ها** و **جزئیات هر تم**.

----------

### بخش ۱: پیش‌نمایش ۵ تم فرعی

-   نمایش **۵ آیکون‌باکس** که نماینده تم‌های فرعی هستند
    
-   هر باکس شامل اطلاعات زیر است:
    
    -   **نام تم استعدادی**
        
    -   **رتبه تم** در بین ۳۴ تم
        
    -   **آیکون اختصاصی تم**
        
    -   **عبارت شاخص تم (Phrase)**
        
-   طراحی هر باکس با **رنگ دامنه (Domain) مرتبط با آن تم** انجام شده است
    
-   هدف این بخش ارائه یک **نمای کلی سریع** از تم‌های فرعی و موقعیت آن‌ها نسبت به تم‌های غالب است
    

----------

### بخش ۲: جزئیات هر تم فرعی

این بخش شامل **۵ سکشن جداگانه** است، یکی برای هر تم فرعی.  


#### محتوای هر سکشن

1.  **تعریف عمومی تم**
    
    -   نمایش توضیح و معرفی عمومی تم استعدادی
        
    -   محتوای این بخش از کلید `onDominanceDescription` در دیتابیس استخراج می‌شود
        
2.  **لیست کاربردهای مهم تم**
    
    -   ارائه کاربردها و نقش‌های مهم استعداد مورد نظر
        
    -   داده‌ها از کلید `howThemeHelpsYou` در دیتابیس دریافت می‌شوند
        
3.  **کارت‌های اطلاعاتی کلیدی**
    
    -   **نام انگلیسی تم**
        
    -   **دسته تم (Domain)**
        
    -   **رتبه تم**  
        این کارت‌ها نمایانگر اطلاعات سریع و شناسایی تم هستند
        
4.  **چارت درصد غلبه تم**
    
    -   نمایش درصد غلبه این تم استعدادی برای کاربر
        
    -   امکان مقایسه بصری با سایر تم‌ها و بررسی شدت غلبه
        

----------

> به طور کلی، این تب مشابه تب بررسی تم‌های برتر است ولی تمرکز بر **تم‌های فرعی و کاربردهای عملی آن‌ها** دارد و طراحی آن برای نمایش سریع و بصری اطلاعات این تم‌ها بهینه شده است.

## تب ۱۰: بررسی نقاط ضعف استعدادی (Weakest Themes Overview)

این تب به تحلیل **۵ تم ضعیف و انتهایی کاربر** می‌پردازد و ساختاری مشابه تب "استعدادهای فرعی" دارد، با تفاوت‌هایی که در نحوه ارائه داده‌ها اعمال شده است.

----------

### بخش ۱: مقدمه هر سکشن

-   هر سکشن مربوط به یک تم ضعیف استعدادی است
    
-   در ابتدای هر سکشن، یک **توضیح عمومی درباره ویژگی‌های احتمالی کاربر با این تم ضعیف** نمایش داده می‌شود
    
-   محتوای این بخش از **کلید `onWeaknessDescription`** در دیتابیس استخراج می‌شود
    

----------

### بخش ۲: تحلیل نقاط ضعف

در این بخش به جای سکشن "کاربردهای مهم استعداد" که در تب استعدادهای فرعی وجود داشت، دو سکشن اصلی قرار دارد:

1.  **موانع و تهدیدها (Obstacles & Threats)**
    
    -   لیستی از جملات که **موانع و چالش‌های مرتبط با این تم ضعیف** را توصیف می‌کند
        
    -   محتوای این بخش از **کلید `onWeaknessObstacles`** در دیتابیس خوانده می‌شود
        
2.  **فرصت‌ها و راهکارها (Opportunities & Recommendations)**
    
    -   لیستی از جملات که **راهکارها و فرصت‌های بهبود مرتبط با این تم ضعیف** را ارائه می‌دهد
        
    -   محتوای این بخش از **کلید `onWeaknessOpportunities`** در دیتابیس دریافت می‌شود
        

----------

> به طور کلی، این تب تمرکز بر **شناخت تم‌های ضعیف کاربر و ارائه تحلیل و توصیه‌های عملی** دارد و طراحی آن برای نمایش سریع و واضح نقاط ضعف و فرصت‌های بهبود بهینه شده است.
---


## جریان داده و رندر نتایج در برنامه

صفحه **نتایج تست کلیفتون (results.php)** به صورت پویا داده‌ها را از سمت سرور دریافت و رندر می‌کند. روند کامل پردازش و انتقال داده‌ها به صورت زیر است:

----------

### ۱. منبع داده‌ها

-   تمامی داده‌های مورد نیاز در **دیتابیس** ذخیره شده‌اند، شامل:
    
    -   تم‌ها (Themes)
        
    -   دامنه‌ها (Domains)
        
    -   جداول استاتیک و محتوای مرتبط با هر تم و دامنه
        

----------

### ۲. درخواست داده‌ها از سمت فرانت‌اند

-   هنگام باز شدن صفحه `results.php`، یک **AJAX Request** به **API پروژه** ارسال می‌شود
    
-   آدرس API فعلی: `api/api.php`
    
-   **ورودی‌ها به API ارسال می‌شوند**:
    
    -   اطلاعات کاربر (User Info)
        
    -   نتایج پاسخ تست (Degrees و انتخاب‌های کاربر)
        

----------

### ۳. پردازش داده‌ها در سمت سرور

-   API دریافت شده را بررسی می‌کند و بر اساس نتیجه تست:
    
    1.  **داده‌های مرتبط با تم‌ها و دامنه‌ها را از دیتابیس fetch می‌کند**
        
    2.  داده‌ها **بر اساس نتیجه کاربر بسته‌بندی می‌شوند**
        
        -   شامل امتیاز هر تم، درصد غلبه دامنه‌ها، رتبه تم‌ها، و اطلاعات توصیفی مورد نیاز هر سکشن
            
    3.  **دیتا به فرمت مشخص برنامه** تبدیل شده و به عنوان response به فرانت ارسال می‌شود
        

----------

### ۴. دریافت و رندر داده‌ها در فرانت‌اند

-   پس از دریافت response از API:
    
    -   فرانت‌اند داده‌ها را **parse** کرده و به تابع اصلی رندر می‌فرستد
        
-   **تابع اصلی رندر**:
    
    -   فایل: `assets/js/main.js`
        
    -   نام تابع: `render_results`
        
    -   وظیفه: رندر کامل محتوای صفحه نتایج شامل تمام تب‌ها و سکشن‌ها بر اساس داده‌های دریافتی
        

----------

### 🔹 نکات مهم

-   تمامی تب‌ها و سکشن‌ها در صفحه `results.php` به صورت **دینامیک** بر اساس داده‌های response ساخته می‌شوند
    
-   هر تغییر در ساختار دیتابیس یا محتوای تم‌ها و دامنه‌ها به صورت خودکار پس از fetch شدن، در فرانت رندر خواهد شد
    
-   این معماری باعث **جدا بودن منطق بک‌اند و فرانت‌اند** و **سهولت نگهداری و بروز رسانی داده‌ها** می‌شود
-   
---


## مدیریت و بارگذاری تصاویر در برنامه

در این پروژه، تمامی تصاویر مورد استفاده در بخش‌های مختلف فرانت‌اند به‌صورت متمرکز و ساختاریافته مدیریت می‌شوند تا هم استفاده از آن‌ها ساده باشد و هم تغییر یا توسعه در آینده به‌راحتی انجام شود.

---

### ۱. محل نگهداری تصاویر

* تمامی تصاویر پروژه در مسیر زیر قرار دارند:

```
assets/images/
```

* این پوشه به‌عنوان **منبع اصلی تصاویر (Image Assets)** در برنامه استفاده می‌شود

---

### ۲. ارتباط تصاویر با ساختار داده‌ها

در ساختار داده‌های برنامه (چه در دیتابیس و چه در فایل‌های JSON)، برای برخی موجودیت‌ها فیلدهایی با نام زیر وجود دارد:

* `picture` (برای تم‌های استعدادی)
* `picture` (برای کتاب‌ها)

ویژگی‌های این فیلدها:

* مقدار آن‌ها **صرفاً نام فایل تصویر به همراه پسوند** است
* مسیر کامل تصویر در دیتابیس ذخیره نمی‌شود
* فایل تصویر مربوطه باید در پوشه `assets/images` (یا زیرپوشه‌های آن) وجود داشته باشد

---

### ۳. ساختار پوشه‌بندی تصاویر

در حال حاضر، پوشه `assets/images` شامل دو پوشه اصلی است:

```
assets/images/
│
├── books/
│   └── تصاویر مربوط به کتاب‌ها
│
└── theme_illustrations/
    └── تصاویر شاخص (Illustration) هر تم استعدادی
```

این ساختار امکان تفکیک منطقی تصاویر بر اساس نوع استفاده را فراهم می‌کند.

---

### ۴. تابع کمکی `getResourceUrl`

در فایل زیر:

```
assets/js/main.js
```

تابعی کمکی با نام زیر تعریف شده است:

```
getResourceUrl()
```

#### وظیفه این تابع:

* دریافت مسیر نسبی فایل تصویر در پوشه `assets/images`
* تبدیل این مسیر به یک **URL معتبر روی سرور**
* بازگرداندن URL نهایی برای استفاده در رندر تصاویر

---

### ۵. فرمت ورودی تابع

ورودی تابع `getResourceUrl` به شکل زیر است:

* مسیر نسبی فایل تصویر
* با این تفاوت که:

  * پوشه‌های تودرتو به‌جای `/` با `.` از هم جدا می‌شوند

#### مثال:

برای تصویر شاخص تم **Achiever** که در مسیر زیر قرار دارد:

```
assets/images/theme_illustrations/achiever.png
```

ورودی تابع به این شکل خواهد بود:

```
getResourceUrl("theme_illustrations.achiever.png")
```

خروجی این تابع:

* یک URL معتبر از تصویر روی سرور
* قابل استفاده مستقیم در تگ‌های `<img>` یا پس‌زمینه‌های CSS

---

### ۶. استفاده در رندر فرانت‌اند

* URL بازگشتی از تابع `getResourceUrl` در فرانت‌اند برای:

  * نمایش تصویر تم‌ها
  * نمایش تصاویر کتاب‌ها
  * سایر المان‌های گرافیکی پویا
    استفاده می‌شود

* این رویکرد باعث می‌شود:

  * وابستگی فرانت‌اند به مسیرهای ثابت کاهش یابد
  * تغییر مسیر یا دامنه سرور بدون نیاز به تغییر داده‌ها امکان‌پذیر باشد
